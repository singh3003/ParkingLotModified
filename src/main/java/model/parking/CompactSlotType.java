package model.parking;

import model.parking.Slot;
import model.parking.SlotType;

public class CompactSlotType extends Slot {
    public CompactSlotType(SlotType slotType) {
        super(SlotType.COMPACT);
    }
}
