package model.parking;

import model.parking.Slot;
import model.parking.SlotType;

public class HandicappedSlotType extends Slot {
    public HandicappedSlotType(SlotType slotType) {
        super(SlotType.HANDICAPPED);
    }
}
