package model.parking;

import model.parking.Slot;
import model.parking.SlotType;

public class LargeSlotType extends Slot {
    public LargeSlotType(SlotType slotType) {
        super(SlotType.LARGE);
    }
}
